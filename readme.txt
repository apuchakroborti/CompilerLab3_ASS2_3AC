how to run code:
1) flex ass.l
2) yacc -d ass.y
3) gcc lex.yy.c y.tab.c -ll -lm
4)./a.out

the expression:
i)new := old+sgn/step;
ii)np, x := trial*trial, 2+3;

Note:semicolon must need and to stop press 'CTRL+C'

